SANITY_STUDIO_PROJECT_ID=your-project-id
SANITY_STUDIO_DATASET=production
# Write token for automatic test document cleanup after e2e runs (get one at sanity.io/manage → API → Tokens)
SANITY_STUDIO_TOKEN=your-api-token
